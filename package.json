{
  "name": "second-sync",
  "version": "0.1.0",
  "description": "Second Sync (秒合わせ) — a second-precise alarm/timer app for reservation calls, LINE/Threads posts, ticket sales, and live starts.",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android",
    "assets": "npx @capacitor/assets generate"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
